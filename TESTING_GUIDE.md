# Руководство по тестированию модульной структуры

## 🚀 Как запустить бота

### 1. Запуск бота:
```bash
cd C:\Users\User\Desktop\bot
python bot_1.py
```

Или через PowerShell:
```powershell
cd C:\Users\User\Desktop\bot
python bot_1.py
```

### 2. Проверка что бот запущен:
- В консоли должно появиться: "Bot started successfully"
- Бот должен отвечать на команды в Telegram

---

## 📸 Как протестировать

### Шаг 1: Отправьте фото в бот
- Откройте Telegram
- Найдите вашего бота
- Отправьте любое фото (JPEG или PNG)

### Шаг 2: Выберите метод
- Бот покажет кнопки с методами
- Выберите: **"ВСЕ МЕТОДЫ ВМЕСТЕ 🔥"** или **"ВСЕ МЕТОДЫ ВМЕСТЕ 🔥 PIXEL"**

### Шаг 3: Выберите количество копий
- Бот покажет кнопки: 1, 5, 10, 20, "Другое число..."
- Выберите, например, **20 копий**

### Шаг 4: Получите результат
- Бот обработает изображение
- Отправит ZIP архив с уникальными копиями

---

## 📊 Что вы получите

### Для метода "ВСЕ МЕТОДЫ ВМЕСТЕ 🔥":

Каждая копия будет иметь **уникальную комбинацию** из:

#### Стандартные методы (всегда применяются):
1. ✅ Metadata (метаданные) - случайные EXIF данные
2. ✅ Micro changes (микро-изменения) - сдвиг пикселей, яркость, цвет
3. ✅ LSB modification (LSB-модификация) - изменение младших битов
4. ✅ ICC Profile (цветовой профиль) - изменение цветового пространства
5. ✅ Method1 (простая обработка) - обрезка, цвет, яркость
6. ✅ Method2 (продвинутая) - масштабирование, гамма, контраст
7. ✅ Method3 (комбинация) - финальная обработка

#### Новые модульные параметры (случайно 3-5 из 8):
1. 🎲 **Bit Depth** (Битовая глубина PNG) - 8 или 16 бит
2. 🎲 **Color Type** (Цветовой тип) - RGB или RGBA
3. 🎲 **PNG Filter** (PNG фильтр) - Adaptive, None, Sub, Up, Average, Paeth
4. 🎲 **Interlace** (Чересстрочная развёртка) - None или Progressive
5. 🎲 **White Balance** (Баланс белого) - 0 (Auto) или 1 (Manual)
6. 🎲 **Aperture** (Диафрагма) - f/1.8, f/2.8, f/4.0, f/5.6, f/8.0, f/11, f/16
7. 🎲 **Resolution** (Разрешение) - 72, 96, 150, 200, 300 DPI
8. 🎲 **Compression** (Сжатие PNG) - уровни 0-9

### Для метода "ВСЕ МЕТОДЫ ВМЕСТЕ 🔥 PIXEL":

**Всё то же самое + Pixel Pattern overlay:**
- Текстовый паттерн из случайных символов
- Alpha 10 (едва заметно)
- Уникальный паттерн для каждой копии

---

## 🔍 Как проверить данные в копиях

### Способ 1: Проверка через Python скрипт

Создайте файл `check_image_data.py`:

```python
import piexif
from PIL import Image
import sys

def check_image_data(image_path):
    """Проверить данные изображения."""
    img = Image.open(image_path)
    
    print(f"\n=== {image_path} ===")
    print(f"Format: {img.format}")
    print(f"Mode: {img.mode}")
    print(f"Size: {img.size}")
    
    # PNG параметры
    if img.format == "PNG":
        print(f"PNG Info: {img.info}")
    
    # EXIF данные
    try:
        exif_dict = piexif.load(image_path)
        
        if "Exif" in exif_dict:
            exif = exif_dict["Exif"]
            if piexif.ExifIFD.WhiteBalance in exif:
                print(f"White Balance: {exif[piexif.ExifIFD.WhiteBalance]}")
            if piexif.ExifIFD.FNumber in exif:
                fnum = exif[piexif.ExifIFD.FNumber]
                print(f"Aperture: f/{fnum[0]/fnum[1]:.1f}")
        
        if "0th" in exif_dict:
            zeroth = exif_dict["0th"]
            if piexif.ImageIFD.XResolution in zeroth:
                xres = zeroth[piexif.ImageIFD.XResolution]
                print(f"Resolution: {xres[0]/xres[1]} DPI")
    except:
        print("No EXIF data")
    
    print()

if __name__ == "__main__":
    if len(sys.argv) > 1:
        check_image_data(sys.argv[1])
    else:
        print("Usage: python check_image_data.py <image_path>")
```

### Способ 2: Проверка через онлайн инструменты

1. **EXIF Viewer** (https://exifdata.com/)
   - Загрузите изображение
   - Увидите все EXIF данные (White Balance, Aperture, Resolution)

2. **PNG Info** (через любой редактор изображений)
   - Откройте PNG в редакторе
   - Проверьте свойства файла

### Способ 3: Проверка через командную строку

```bash
# Для Windows (если установлен ImageMagick)
identify -verbose image.jpg

# Или через Python
python -c "from PIL import Image; img = Image.open('image.jpg'); print(img.info)"
```

---

## 📋 Пример результата

Если вы запросите **20 копий**, вы получите:

### ZIP архив с 20 файлами:
- `photo_1_of_20.jpg` - с комбинацией параметров A
- `photo_2_of_20.jpg` - с комбинацией параметров B
- `photo_3_of_20.jpg` - с комбинацией параметров C
- ... и так далее до 20

### Каждая копия будет отличаться:
- ✅ Разными метаданными (EXIF)
- ✅ Разными значениями параметров (3-5 случайных модулей)
- ✅ Разными pixel patterns (для PIXEL метода)
- ✅ Разными хешами (MD5)

---

## 🧪 Быстрый тест

1. **Запустите бота:**
   ```bash
   python bot_1.py
   ```

2. **Отправьте фото в Telegram боту**

3. **Выберите:**
   - Метод: "ВСЕ МЕТОДЫ ВМЕСТЕ 🔥 PIXEL"
   - Количество: 5 копий

4. **Получите ZIP с 5 файлами**

5. **Распакуйте и проверьте:**
   - Откройте каждый файл
   - Проверьте свойства (правой кнопкой → Свойства)
   - Увидите разные EXIF данные в каждом файле

---

## ✅ Ожидаемый результат

- ✅ **20 копий** = 20 разных файлов
- ✅ Каждый файл имеет **уникальную комбинацию** параметров
- ✅ Все файлы в **ZIP архиве**
- ✅ Каждый файл **визуально похож**, но **технически уникален**


