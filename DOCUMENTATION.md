# Документация: Telegram Bot для Уникализации Изображений

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Запуск бота](#запуск-бота)
3. [Работа с ботом](#работа-с-ботом)
4. [Кнопки и их функции](#кнопки-и-их-функции)
5. [Методы уникализации](#методы-уникализации)
6. [Архитектура проекта](#архитектура-проекта)
7. [Команды бота](#команды-бота)

---

## 🎯 Обзор проекта

Telegram-бот для уникализации изображений. Бот изменяет изображения так, чтобы они визуально оставались прежними, но бинарно отличались (другой хеш), что полезно для обхода дубликатов в системах.

**Основные возможности:**
- 9 различных методов уникализации
- Генерация от 1 до 100 уникальных копий
- Пакетная обработка до 10 изображений
- Сохранение высокого качества (SSIM >= 0.99)
- Автоматическая генерация случайных имён файлов

---

## 🚀 Запуск бота

### Требования
- Python 3.10+
- Установленные зависимости из `requirements.txt`

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск
```bash
python bot_1.py
```

**Важно:** Убедитесь, что переменная окружения `BOT_TOKEN` установлена, или она будет взята из `bot_1.py`.

---

## 💬 Работа с ботом

### Шаг 1: Запуск бота
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`

### Шаг 2: Отправка изображения
Отправьте боту изображение одним из способов:
- **Как фото** (сжатое Telegram) - отправьте через "Фото"
- **Как документ** (без сжатия) - отправьте через "Документ"

**Поддерживаемые форматы:**
- JPEG (.jpg, .jpeg)
- PNG (.png)

**Ограничения:**
- Максимальный размер: 20 МБ
- Анимированные изображения (GIF) не поддерживаются

### Шаг 3: Выбор метода уникализации
После отправки изображения бот покажет кнопки с методами уникализации.

### Шаг 4: Выбор количества копий
После выбора метода бот предложит выбрать количество копий (1, 5, 10, 20 или произвольное число).

### Шаг 5: Получение результата
- **1 копия** → отправляется одиночный файл
- **>1 копии** → отправляется ZIP-архив со всеми копиями

---

## 🔘 Кнопки и их функции

### Кнопки выбора метода уникализации

После отправки изображения появляются следующие кнопки:

| Кнопка | Callback Data | Описание |
|--------|---------------|----------|
| **Только метаданные** | `method:metadata` | Заменяет только EXIF/IPTC/XMP метаданные. Не изменяет пиксели. Максимальное качество. |
| **Микро-изменения** | `method:micro` | Применяет незаметные изменения: сдвиг на 1-2 пикселя, микро-коррекция яркости/цвета (±0.5%). |
| **LSB-модификация** | `method:lsb` | Изменяет младшие биты пикселей (5% пикселей). Математически гарантированная уникальность. |
| **ICC цветовой профиль** | `method:icc_profile` | Меняет цветовой профиль изображения (sRGB, AdobeRGB и др.). |
| **Простая (Метод 1)** | `method:method1` | Обрезка, коррекция цвета/яркости, добавление EXIF метаданных. |
| **Продвинутая (Метод 2)** | `method:method2` | 6 вариантов с зеркалированием, округлением углов, PNG-конвертацией. |
| **Комбинация 1+2 (Метод 3)** | `method:method3` | Комбинация методов 1 и 2. Все возможности вместе. |
| **ВСЕ МЕТОДЫ ВМЕСТЕ 🔥** | `method:all_combined` | Применяет последовательно: 1) Combined (micro → lsb → metadata), 2) ICC Profile, 3) Method1, 4) Method2 (1 вариант), 5) Method3 (1 вариант). |
| **ВСЕ МЕТОДЫ ВМЕСТЕ 🔥 PIXEL** | `method:all_combined_with_pixel` | Применяет все методы из "ВСЕ МЕТОДЫ ВМЕСТЕ 🔥", затем добавляет pixel pattern overlay (alpha 10). При генерации множественных копий каждый вариант обрабатывается независимо. |

### Кнопки выбора количества копий

После выбора метода появляются кнопки:

| Кнопка | Callback Data | Результат |
|--------|---------------|-----------|
| **1** | `count:1` | Генерирует 1 копию. Отправляется одиночный файл. |
| **5** | `count:5` | Генерирует 5 копий. Отправляется ZIP-архив. |
| **10** | `count:10` | Генерирует 10 копий. Отправляется ZIP-архив. |
| **20** | `count:20` | Генерирует 20 копий. Отправляется ZIP-архив. |
| **Другое число...** | `count:custom` | Запрашивает ввод произвольного числа (1-100). |

### Кнопки предпросмотра (если включён режим `/preview`)

| Кнопка | Callback Data | Действие |
|--------|---------------|----------|
| **✓ Подтвердить** | `preview:confirm` | Отправляет полноразмерный файл. |
| **✗ Отмена** | `preview:cancel` | Отменяет обработку. |

---

## 🔧 Методы уникализации

### 1. Только метаданные (Metadata)
- **Что делает:** Заменяет EXIF/IPTC/XMP метаданные на случайные значения
- **Изменяет пиксели:** Нет
- **Качество:** SSIM = 1.0 (идеально)
- **Скорость:** Очень быстро
- **Когда использовать:** Когда нужно максимальное качество, а уникальность нужна только на уровне хеша

### 2. Микро-изменения (Micro)
- **Что делает:** 
  - Сдвиг на 1-2 пикселя от краёв
  - Коррекция яркости ±0.5%
  - Коррекция цвета ±0.5%
- **Изменяет пиксели:** Да, минимально
- **Качество:** SSIM >= 0.995
- **Скорость:** Быстро
- **Когда использовать:** Баланс между уникализацией и качеством

### 3. LSB-модификация
- **Что делает:** Изменяет младшие биты 5% пикселей (±1 в значении)
- **Изменяет пиксели:** Да
- **Качество:** SSIM >= 0.999 (незаметно)
- **Скорость:** Средне
- **Когда использовать:** Когда нужна гарантированная уникальность с минимальным визуальным воздействием

### 4. ICC цветовой профиль
- **Что делает:** Меняет цветовой профиль (sRGB → AdobeRGB → Rec709 и т.д.)
- **Изменяет пиксели:** Нет (только метаданные профиля)
- **Качество:** SSIM = 1.0
- **Скорость:** Быстро
- **Когда использовать:** Для изменения цветового пространства

### 5. Простая (Метод 1)
- **Что делает:** 
  - Обрезка краёв
  - Коррекция цвета/яркости
  - Добавление EXIF метаданных
- **Изменяет пиксели:** Да
- **Качество:** SSIM >= 0.99
- **Скорость:** Средне
- **Когда использовать:** Для базовой уникализации с простыми изменениями

### 6. Продвинутая (Метод 2)
- **Что делает:** 
  - 6 вариантов обработки
  - Зеркалирование (горизонтальное/вертикальное)
  - Округление углов
  - PNG-конвертация
- **Изменяет пиксели:** Да
- **Качество:** SSIM >= 0.99
- **Скорость:** Медленно
- **Когда использовать:** Когда нужны визуально разные варианты

### 7. Комбинация 1+2 (Метод 3)
- **Что делает:** Применяет метод 2, затем метод 1 к каждому варианту
- **Изменяет пиксели:** Да
- **Качество:** SSIM >= 0.99
- **Скорость:** Медленно
- **Когда использовать:** Максимальная вариативность

### 8. ВСЕ МЕТОДЫ ВМЕСТЕ 🔥
- **Что делает:** Применяет все методы последовательно к одному изображению:
  1. **Combined** (внутри: micro → lsb → metadata):
     - Micro: сдвиг 0-1 пиксель, яркость ±0.3%, цвет ±0.3%
     - LSB: модификация 3% пикселей, 1 бит
     - Metadata: замена EXIF/IPTC/XMP
  2. **ICC Profile** - смена цветового профиля
  3. **Method1** - обрезка краёв (0-3 пикселя), коррекция цвета/яркости (±2%), EXIF
  4. **Method2** (берётся 1-й вариант) - обрезка (2-2.5%), scale jitter (±3%), gamma/contrast, зеркалирование (опционально), округление углов (опционально)
  5. **Method3** (берётся 1-й вариант) - внутри Method3: применяет Method2 к изображению, затем к результату применяет Method1 enhancements (цвет/яркость ±2%, EXIF)
- **Изменяет пиксели:** Да
- **Качество:** SSIM >= 0.99
- **Скорость:** Очень медленно
- **Когда использовать:** Максимальная уникализация, когда качество не критично

### 9. ВСЕ МЕТОДЫ ВМЕСТЕ 🔥 PIXEL
- **Что делает:** 
  - Для 1 копии: применяет все методы из "ВСЕ МЕТОДЫ ВМЕСТЕ 🔥", затем добавляет pixel pattern overlay (alpha 10)
  - Для множественных копий: каждый вариант обрабатывается полностью независимо (вызывается `all_combined.process()` для каждого), затем к каждому применяется pixel pattern
- **Pixel Pattern:** наложение текстового паттерна из случайных символов с прозрачностью 10/255 (едва заметно)
- **Изменяет пиксели:** Да
- **Качество:** SSIM >= 0.99
- **Скорость:** Очень медленно (особенно для множественных копий, т.к. каждый вариант обрабатывается заново)
- **Когда использовать:** Абсолютная максимальная уникализация

---

## 🏗️ Архитектура проекта

```
bot/
├── bot_1.py              # Точка входа (backwards compatibility)
├── bot.py                # Основной файл бота
├── requirements.txt      # Зависимости
│
├── src/
│   ├── bot.py           # Telegram bot handlers и команды
│   ├── config.py        # Конфигурация (лимиты, таймауты)
│   │
│   ├── handlers/
│   │   ├── photo.py     # Обработка фото/документов
│   │   └── callbacks.py # Обработка inline-кнопок
│   │
│   ├── uniqueizers/     # Алгоритмы уникализации
│   │   ├── base.py      # Базовый класс
│   │   ├── metadata.py  # Метод метаданных
│   │   ├── micro.py     # Микро-изменения
│   │   ├── lsb.py       # LSB-модификация
│   │   ├── method1.py   # Метод 1
│   │   ├── method2.py   # Метод 2
│   │   ├── method3.py   # Метод 3
│   │   ├── icc_profile.py
│   │   ├── combined.py  # Комбинированный
│   │   ├── all_combined.py
│   │   ├── all_combined_with_pixel.py
│   │   └── pixel_pattern.py
│   │
│   └── utils/
│       ├── image.py      # Утилиты для работы с изображениями
│       ├── metadata.py   # Работа с EXIF/IPTC/XMP
│       ├── filename.py  # Генерация случайных имён файлов
│       ├── archive.py    # Создание ZIP-архивов
│       └── ...
│
└── specs/               # Документация и спецификации
```

---

## 📱 Команды бота

### `/start`
**Описание:** Приветствие и инструкции по использованию бота.

**Что показывает:**
- Список доступных методов уникализации
- Поддерживаемые форматы
- Ограничения по размеру

**Использование:**
```
/start
```

### `/help`
**Описание:** Показывает ту же информацию, что и `/start`.

**Использование:**
```
/help
```

### `/preview`
**Описание:** Переключает режим предпросмотра.

**Режимы:**
- **Выключен (по умолчанию):** Результат отправляется сразу
- **Включён:** Сначала показывается уменьшенная версия, затем можно подтвердить отправку полноразмерного файла

**Использование:**
```
/preview
```

**Пример работы:**
1. Отправьте `/preview` → режим включён
2. Отправьте изображение и выберите метод
3. После обработки увидите превью
4. Нажмите "✓ Подтвердить" для получения полного файла

---

## 🔄 Процесс обработки изображения

### Полный цикл обработки

```
1. Пользователь отправляет изображение
   ↓
2. Бот скачивает файл и проверяет формат/размер
   ↓
3. Создаётся сессия обработки (в памяти)
   ↓
4. Показываются кнопки выбора метода
   ↓
5. Пользователь выбирает метод (или таймаут 30 сек → метод по умолчанию)
   ↓
6. Показываются кнопки выбора количества копий
   ↓
7. Пользователь выбирает количество (или таймаут 30 сек → 1 копия)
   ↓
8. Генерация уникальных копий
   ↓
9. Проверка уникальности хешей
   ↓
10. Отправка результата:
    - 1 копия → одиночный файл
    - >1 копии → ZIP-архив
```

### Таймауты

- **Выбор метода:** 30 секунд
  - Если не выбран → применяется метод по умолчанию (`all_combined`)
  
- **Выбор количества копий:** 30 секунд
  - Если не выбрано → генерируется 1 копия

- **Ввод произвольного числа:** 60 секунд
  - Если не введено → сессия истекает

---

## 📦 Формат результатов

### Одиночный файл (1 копия)

**Имя файла:** `photo_xxxx_yyyy.jpg` (случайное)
**Формат:** Оригинальный формат (JPEG/PNG)
**Подпись:** "Уникализированное изображение\nМетод: [название метода]"

### ZIP-архив (>1 копии)

**Имя архива:** `photo_xxxx_yyyy_unique_N.zip` (N = количество копий)
**Содержимое:**
- `photo_01_xxxx.jpg`
- `photo_02_yyyy.jpg`
- ...
- `photo_NN_zzzz.jpg`

**Подпись:** "Архив с N уникальными копиями\nМетод: [название метода]"

---

## ⚙️ Конфигурация

Основные настройки находятся в `src/config.py`:

```python
MAX_FILE_SIZE_MB = 20          # Максимальный размер файла
MAX_BATCH_SIZE = 10             # Максимум изображений в пакете
MAX_COPY_COUNT = 100            # Максимум копий за раз
MIN_SSIM = 0.99                # Минимальное качество (SSIM)
METHOD_SELECTION_TIMEOUT = 30  # Таймаут выбора метода (сек)
COUNT_SELECTION_TIMEOUT = 30   # Таймаут выбора количества (сек)
DEFAULT_METHOD = "all_combined" # Метод по умолчанию
DEFAULT_COPY_COUNT = 1          # Количество по умолчанию
```

---

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте, что бот запущен
- Проверьте токен в `BOT_TOKEN`
- Проверьте логи на наличие ошибок

### Изображение не обрабатывается
- Проверьте формат (только JPEG/PNG)
- Проверьте размер (максимум 20 МБ)
- Убедитесь, что изображение не повреждено

### Получаю только 1 копию вместо нескольких
- Проверьте, что выбрали количество > 1
- Убедитесь, что не истёк таймаут (30 сек)
- Для метода "ВСЕ МЕТОДЫ ВМЕСТЕ 🔥 PIXEL" обработка может занять время

### Бот зависает при генерации множественных копий
- Методы "all_combined" и "all_combined_with_pixel" очень тяжёлые
- Для 20+ копий используйте более лёгкие методы (metadata, micro, lsb)
- Или уменьшите количество копий

---

## 📝 Примеры использования

### Пример 1: Быстрая уникализация с максимальным качеством
1. Отправьте изображение
2. Выберите "Только метаданные"
3. Выберите "1"
4. Получите файл с идеальным качеством

### Пример 2: Генерация 20 вариантов для маркетинга
1. Отправьте изображение
2. Выберите "Микро-изменения" (быстро) или "LSB-модификация"
3. Выберите "20"
4. Получите ZIP-архив с 20 уникальными копиями

### Пример 3: Максимальная уникализация
1. Отправьте изображение
2. Выберите "ВСЕ МЕТОДЫ ВМЕСТЕ 🔥 PIXEL"
3. Выберите "1" (для множественных копий это очень долго)
4. Дождитесь результата (может занять время)

---

## 🔒 Безопасность

- **Токен бота:** Хранится в переменной окружения или в `bot_1.py` (для разработки)
- **Данные:** Все данные хранятся только в памяти во время обработки
- **Сессии:** Автоматически очищаются после обработки или таймаута
- **Файлы:** Не сохраняются на диск, только обрабатываются в памяти

---

## 📚 Дополнительная информация

- **Спецификации:** См. `specs/001-improve-uniqueization/`
- **Тесты:** См. `tests/`
- **Исходный код:** См. `src/`

---

**Версия документации:** 1.0  
**Дата обновления:** 2025-11-29

